# Code Review: E05-S01 — Daily Study Streak Counter (v2)

**Date**: 2026-03-05
**Reviewer**: code-review agent (Opus)
**Files reviewed**: studyLog.ts, StudyStreak.tsx, StudyStreakCalendar.tsx, Overview.tsx, studyLog.test.ts, story-e05-s01.spec.ts

## Summary

Re-review after fixing all blocker and high-priority findings from v1. Previous high-priority issues (UTC timezone bug, DST compound bug, cn() usage, missing unit tests) all resolved. New findings are lower severity.

## Findings

### Blocker
1. **Uncommitted working tree changes** — all files
   - All fixes exist only in working tree, not committed. Process concern — will be handled by `/finish-story`.
   - **Assessment**: Not a code blocker. Normal workflow — commits happen at ship time.

### High Priority
1. **Triple localStorage parse on every render** — StudyStreakCalendar.tsx:39-42
   - `getCurrentStreak()`, `getLongestStreak()`, `getStudyActivity()`, `getStreakPauseStatus()` each parse `study-log` from localStorage independently. On large logs this is redundant work.
   - **Assessment**: Low risk for current data sizes. Could memoize if performance becomes an issue. Not blocking.
   - Confidence: 55/100

2. **key={streakVersion} remount strategy** — Overview.tsx
   - Using key prop to force remount of `StudyStreakCalendar` on study-log-updated events. This unmounts/remounts the entire subtree including dialog state.
   - **Assessment**: Works correctly for current implementation. The alternative (granular state updates) would be more complex. Acceptable trade-off.
   - Confidence: 50/100

3. **Redundant motion-reduce:animate-none** — StudyStreak.tsx
   - **Status: Already fixed in working tree** (removed during v1 fix pass)

### Medium
1. **grid-cols-6 mobile fit** — StudyStreakCalendar.tsx:85
   - With 30 cells in a 6-column grid, that's 5 rows on mobile. Works but creates a taller grid.
   - **Status: Fixed during design review v2** — changed from grid-cols-10 to grid-cols-6 sm:grid-cols-10

2. **DST edge in pause expiry** — studyLog.ts (getStreakPauseStatus)
   - `Math.floor` for elapsed days could be off by 1 during DST transitions.
   - **Assessment**: Low impact — pause is a coarse-grained feature (days, not hours). Edge case unlikely to cause user-visible issues.
   - Confidence: 45/100

3. **Pulse effect vs key remount race** — StudyStreak.tsx + Overview.tsx
   - The pulse animation triggers on mount when `current > 0`, but key remount means the component always mounts fresh. The 600ms timeout cleanup runs correctly because it's tied to the effect lifecycle.
   - **Assessment**: Works correctly. The animation fires on remount which is the desired behavior.
   - Confidence: 40/100

### Nits
1. **`size-*` Tailwind shorthand** — various files
   - **Status: Already fixed in working tree**

2. **Keyframes in tailwind.css** — src/styles/tailwind.css
   - `streak-pulse` keyframes defined in global CSS. Could use Tailwind v4 `@theme` but current approach works fine.
   - Confidence: 25/100

3. **`forceUpdate` pattern** — StudyStreakCalendar.tsx:44
   - `useState(0)` + `forceUpdate(v => v + 1)` is a common React pattern but slightly unusual. Could use `useReducer` for clarity.
   - Confidence: 20/100
