# Code Review: E05-S05 — Study Reminders & Notifications

**Date**: 2026-03-07
**Reviewed By**: Claude Code (code-review agent)

## What Works Well

- Clean architecture separation: pure lib → hook → component follows Epic 5 pattern
- Multi-tab dedup via localStorage date strings is elegant
- Permission flow handles edge cases (unsupported, denied, granted)

## Blockers

1. **Uncommitted implementation (confidence: 99)**: All 3 new files are untracked; Layout.tsx and Settings.tsx changes are unstaged. Must commit before merge.

2. **AC4 and AC5 have zero test coverage (confidence: 95)**: Core notification delivery paths are completely untested.

## High Priority

3. **`handleStudyUpdate` is a dead no-op (confidence: 95)**: `useStudyReminders.ts:84-87` — listener body is empty. Remove or implement.

4. **`isPaused` has no reactivity (confidence: 85)**: `ReminderSettings.tsx:21` — reads once on render, no event listener for pause changes.

5. **Zero unit tests for all new files (confidence: 90)**: `studyReminders.ts` has 8 exported pure functions ideal for unit testing.

## Medium

6. **No validation on `dailyReminderTime.split(':')` (confidence: 72)**: Could produce NaN, silently preventing notification.

7. **`w-4 h-4` instead of `size-4` (confidence: 90)**: Three Lucide icons use old pattern.

8. **Hardcoded colors (confidence: 75)**: `text-green-600` and `text-amber-600` instead of theme tokens.

9. **Redundant render from event loop (confidence: 72)**: `update()` → `saveReminderSettings()` → event → `setSettings()` causes extra render.

10. **AC7 test seeds pause data after page load (confidence: 78)**: Component has already read `isPaused = false` by seed time.

## Nits

- Re-export of `getCurrentStreak` adds indirection
- `focus:` instead of `focus-visible:` on time input
- Midnight wraparound bug in ±1 minute matching
